<div class="container" style="margin-top: 10%;">
  <div class="d-flex" style="width: 500px; margin: 0 auto">
    <% if @enrollment.completed %>
      <div>
        <h1 class="border-bottom">Parabéns!</h1>
        <h3>Você foi aprovado em <%= @enrollment.course.name %>, tendo acertado <%= @result %> dentre <%= @total %> questões.</h3>
        <%= link_to "Certificado", downloadpdf_path(@enrollment), class: "btn btn-primary col-sm mb-3" %>
        <%= link_to "Volte ao curso", course_path(@enrollment.course), class: "btn btn-primary col-sm" %>
      </div>
    <% else %>
      <div class="justify-content-center">
        <p>Você acertou <%= @result %> dentre <%= @total %> questões.</p>
        <p>Infelizmente não foi desta vez.</p>
        <%= link_to "Volte ao curso", course_path(@enrollment.course), class: "btn btn-primary col-sm" %>
      </div>
    <% end %>
  </div>
</div>
